# Frontend env (build-time)
# LOCAL: เว้นว่างได้ โค้ด fallback เป็น http://localhost:8080/api/v1 เอง
# PROD (Vercel): ตั้งใน Project Settings > Environment Variables แล้ว redeploy
#   REACT_APP_API_URL=https://<your-backend>.onrender.com/api/v1
REACT_APP_API_URL=
